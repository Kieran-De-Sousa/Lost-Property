using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InvHandler : MonoBehaviour
{
    GameObject[,] storage;
    
    // Start is called before the first frame update
    void Start()
    {
        storage = new GameObject[4, 3];
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
